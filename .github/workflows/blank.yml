
name: Sync with Artifactory

on: 
  workflow_dispatch:
  schedule:
    - cron: '7 15 * * *'

jobs:
  update-main:
    name: Sync main branch
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Add from git
        run: |
          cd /tmp/
          git clone https://github.com/ARM-software/ML-examples
          cp /tmp/ML-examples/cmsis-pack-examples .
          git commit -m "uto-sync-test"
          git push
